package algorithm.one;

/**
 * 查找第一个公共节点
 */
public class FindFirstSameNode {

    public static void main(String[] args){
        Node pHead1 = Node.createLink(10);
        Node pHead2 = Node.createLink(7);
        int length1 = Node.calcLength(pHead1);
        int length2 = Node.calcLength(pHead2);
        if(length1 > length2){

        }
    }
}
